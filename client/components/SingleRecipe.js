import React, { Component } from 'react'
import { Text, View, Dimensions, ImageBackground } from 'react-native'

const SingleRecipe = (props) => (<Text>This is a component for recipe single </Text>)
export default SingleRecipe;
